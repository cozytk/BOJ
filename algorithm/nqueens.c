#include <stdio.h>
#include <unistd.h>

int	g_arr[15];
int	g_pos[15][15];
int	g_count = 0;
int	k;

void	checknode(int row)
{
	int col = 0;

	while (col < k)
	{
		g_arr_gen(row);
			
int	main(void)
{
	scanf("%d", &k);
	checknode(0);
	printf("%d\n", g_count);
	return (0);
}
